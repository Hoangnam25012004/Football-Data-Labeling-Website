# Sample / large match data — not part of the website
*.xlsx
~$*

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Build output (generated by CI)
_site/

# npm dependencies (restored with `npm install`; see worker/package-lock.json)
node_modules/
